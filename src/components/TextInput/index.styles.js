import styled from "styled-components";

const CaptionEl = styled.span`
  display: block;
  margin-left: auto;
`;

const WrapperEl = styled.div`
  display: flex;
  border: 2px solid black;
  border-radius: 7px;
  padding: 5px 9px;
`;

export { WrapperEl, CaptionEl };
